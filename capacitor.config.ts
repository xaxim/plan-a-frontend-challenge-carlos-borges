import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plan-a-frontend-challenge-carlos-borges',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
